Unattended-Upgrade::Automatic-Reboot "{{ system.unattended_upgrades.config.auto_reboot.enabled | lower }}";
Unattended-Upgrade::Automatic-Reboot-Time "{{ system.unattended_upgrades.config.auto_reboot.time }}";

{% if system.unattended_upgrades.mail.enable %}
Unattended-Upgrade::Mail "{{ system.unattended_upgrades.mail.to }}";
{% if system.unattended_upgrades.mail.only_on_error %}
Unattended-Upgrade::MailOnlyOnError "true";
{% endif %}
{% endif %}

Unattended-Upgrade::Allowed-Origins {
  "${distro_id} ${distro_codename}-security";
{% if not system.unattended_upgrades.security_only %}
  "${distro_id} ${distro_codename}-updates";
{% endif %}
};

Unattended-Upgrade::Package-Blacklist{
{% for package in system.unattended_upgrades.blacklist %}
      "{{ package }}";
{% endfor %}
}

Unattended-Upgrade::AutoFixInterruptedDpkg "true";

Unattended-Upgrade::Remove-Unused-Dependencies "{{ system.unattended_upgrades.auto_clean | lower }}";
